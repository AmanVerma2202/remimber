# Server environment variables
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/noteapp

# JWT secret for signing cookies (use a long random string in production)
JWT_SECRET=change_me_dev_secret

# Where the SPA lives (for CORS + redirects after auth)
CLIENT_URL=http://localhost:5173
DEV_MODE=
NODE_ENV=